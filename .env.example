# --- Server-Side Only (Secrets) ---
# These are NEVER exposed to the frontend bundle.
# They are only accessible by Netlify Functions.

# Firebase - Project Config
# Note: Remove VITE_ prefix to ensure Vite does NOT bundle them.
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# Admin Access
ADMIN_EMAILS=your.email@gmail.com,admin@example.com

# AI Keys
GEMINI_API_KEY=
